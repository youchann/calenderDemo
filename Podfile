# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'calenderDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'FSCalendar'
  pod 'CalculateCalendarLogic'
  pod 'RealmSwift'
  platform :ios, '10.0'
  
  # Pods for calenderDemo

  target 'calenderDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'calenderDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
